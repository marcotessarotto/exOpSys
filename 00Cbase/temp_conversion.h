#ifndef TEMP_CONVERSION_H_   // preprocessore: se TEMP_CONVERSION_H_ non è definito.... includi il codice fino a endif
#define TEMP_CONVERSION_H_   // preprocessore: definisci una macro di nome "TEMP_CONVERSION_H_"

// questo è un "INCLUDE FILE" (file di inclusione), ovvero con estesione .h

// nei file di inclusione scriviamo le DEFINIZIONI DI FUNZIONE (il "contratto")
// le DICHIARAZIONI di FUNZIONE (cioè l'implementazione) le mettiamo nei file .c

void temperature_conversion_int();

void temperature_conversion_float();

void temperature_conversion_float_for();


void temperature_conversion_celsius_to_fahrenheit();


#endif /* TEMP_CONVERSION_H_ */
